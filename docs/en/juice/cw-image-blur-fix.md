# Sensitive image display fix

Upstream Misskey's 2026.7.0 series had a bug where the sensitive image blurhash placeholder would not render correctly right after opening a CW (Content Warning), or when switching tabs.

Juice Server has this bug fixed — the sensitive image blur displays correctly both when opening a CW and when switching tabs.
