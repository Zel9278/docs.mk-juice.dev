# 更新履歴

misskey-juiceのJUICE独自機能に関する主な変更履歴です。本家Misskey由来の変更は含みません。全履歴は[GitHubのリリースページ](https://github.com/Zel9278/misskey-juice/releases)をご覧ください。

## v2026.9.0-juice+3.0

### Client
- Fix: 投稿フォームの「…」メニューにある投稿の言語設定にJUICEバッジが表示されていなかった問題を修正。`MkMenu.vue`のメニュー項目のうちtype指定が無い通常のボタン項目(type: 'parent'/'link'以外)には、そもそもバッジを描画するテンプレート自体が無かった(型定義上も`badge`フィールドが存在しなかった)ため、対応を追加した
- Fix: 「サーバー情報」ページの「JUICEについて」リンク、および`MkMenu.vue`のメニュー項目全般で、JUICEバッジの上端が省略記号用のoverflow: hiddenによって欠けて見えていた問題を修正。MkFolder等では既に対応済みだったが、FormLink(form/link.vue)・MkMenu.vueには同じ対応が漏れていた(タイムラインの言語フィルタ・リレー絞り込み等、既存のtype: 'parent'バッジにも影響していた)
- Fix: 投稿フォームの「AI生成物」ボタン、および添付ファイル・ドライブ・ノートの「…」メニューにあるAI生成物のマーク切り替え・絞り込み項目に、JUICE独自機能であることを示す表示が無かった問題を修正。あわせて`MkMenu.vue`のtype: 'a'/'switch'/'radio'の各メニュー項目でJUICEバッジ自体が描画されていなかった(type: 'parent'/'link'のみ対応)不具合も修正し、今後のJUICE独自メニュー項目全般で正しく表示されるようにした。投稿フォームの「AI生成物」ボタン本体はアイコンのみのため、文字バッジではなくアイコン専用タブと同じ小さいドットで表示
- Fix: プロフィール画面のニックネーム機能(JUICE独自)が、本家の「メモ」機能と全く同じ見た目で隣接表示されているのに、JUICE独自であることを示す表示が無かった問題を修正
- Fix: 「利用可能なリアクションを先頭に表示する」設定で使われる並び替え用の判定が、ロール制限・センシティブ・ローカル限定といった実際の権限チェックを行っておらず、クリックできない(権限が無い)リアクションが「利用可能」として先頭に表示されうる不整合を修正。リアクションボタン自体のクリック可否判定と同じロジックに統一した
- Enhance: ジョブキューウィジェットで鳴らす音を、鳴らす/鳴らさないの切り替えだけでなく、好きなサウンドに変更できるように(JUICE独自)
- Enhance: 承認式新規登録で、登録後の審査待ち画面・サインイン時の未承認エラーの両方から、審査状況確認ページ(/signup-check)へ直接移動できるように(JUICE独自)。今までは確認コードがこの端末に自動保存されるだけで、確認ページへの導線が画面上に無かった
- Fix: お知らせへのリアクションが、「リアクションする際確認する」設定を無視して常に確認無しで即反映されていた問題を修正。ノートへのリアクションでは正しく確認ダイアログが出ていたが、お知らせのリアクション(MkAnnouncementReactions.vue)だけこのチェックが実装されていなかった
- Enhance: アバターデコレーション申請フォーム・審査画面のプレビュー機能を強化(JUICE独自)。装着ダイアログと同じ角度・位置(X/Y)・反転の調整UIを追加し、明背景・暗背景それぞれでの見え方も並べて確認できるように。今までは角度・位置が常に固定(0)のプレビューしか見られなかった。角度・位置の調整はあくまでプレビュー確認用で、申請データ自体には含まれない(実際の角度・位置は装着する各ユーザーが個別に設定するもの)
- Fix: タブバー(みつけるのランキングタブ等)・折りたたみセクション(MkFolder、コントロールパネルの外部サービス設定等)で、周囲のテキストより背の高いバッジ(JUICEバッジ・betaバッジ等)の上端が、省略記号・スクロール用のoverflow: hiddenによって欠けて見えていた問題を修正
- Feat: ロール単位で絵文字申請・アバターデコレーション申請・承認式新規登録の承認権限を渡されたモデレーター以外のユーザーでも、コントロールパネル(モデレーター専用)を経由せずに各承認画面へ到達できるよう、「ツール」メニューに専用リンクを追加(JUICE独自、カスタム絵文字管理・アバターデコレーション管理と同じ方式)
- Feat: ノート検索画面に、AND/OR切り替え・除外ワード・添付ファイル有無・CW有無・返信有無・投票有無・公開範囲での絞り込みができる高度な検索オプションを追加(JUICE独自、misskey-tempuraを参考に実装。ただし本家のクエリ構文をそのままパースする方式は構文エラーのリスクがあるため採用せず、従来通りパースを経由しない安全な方式のまま機能を拡張)
- Feat: 検索ページに、自分が付けたリアクションでノートを絞り込む専用タブ「リアクション」を追加(JUICE独自)。「何かしらリアクションした投稿」または特定のリアクションを選んでの絞り込みに、キーワード検索・ノート検索タブと同じ高度な検索オプション(OR検索・除外ワード・各種フィルタ)・日時範囲・検索範囲(全体/ローカル/サーバー指定/ユーザー指定)・URL/@ユーザー/#タグの自動リンクも組み合わせ可能。プライバシー上、自分自身のリアクションのみが対象
- Fix: タブバーを下部に表示する設定(「タブバーを下に表示する」)を有効にしていると、JUICE独自のタブ(タイムライン・エクスプローラー・検索の各種タブ等)に付くはずのJUICEバッジが表示されない問題を修正。上部のタブバーでは既に対応済みだったが、下部タブバー用のコンポーネントには同じ対応が漏れていた
- Fix: 数式(KaTeX)を深くネストさせた投稿(継続分数を延々ネストする、`\sum`の上下添字を延々入れ子にする等)を表示すると、生成されるHTMLがネスト段数に比例して肥大化し、ブラウザのタブがフリーズ・クラッシュしうる問題を修正。数式のネスト深度・文字数が一定を超える場合はレンダリングを試みず、通常のパース失敗時と同じく数式をテキストのまま表示するようにした(JUICE独自)
- Enhance: 絵文字申請フォームに、申請中の画像をノートにリアクションした場合の見た目のプレビューを追加(JUICE独自)。アバターデコレーション申請の「自分のアバターに重ねて表示」と同じ考え方で、承認前でも実際の見た目を確認できる
- Enhance: 絵文字申請・アバターデコレーション申請で、複数の画像をまとめて選択すると同じ画面から複数件をまとめて申請できるように(JUICE独自)。ロールポリシーで設定できる同時申請数の上限まで、1回の操作でまとめて送信可能
- Fix: 「自分の申請一覧」(絵文字申請・アバターデコレーション申請)で、申請した画像そのものではなく汎用アイコンしか表示されていなかった問題を修正。管理画面の審査一覧では元々サムネイル表示されていたが、申請者自身の一覧には同じ情報が渡っていなかった
- Feat: JUICE設定に、ユーザーランキング(投稿数・リアクション数)の表示人数を変更できる設定を追加(JUICE独自、既定3人)。今まで3人固定だった
- Feat: 承認式新規登録で、却下時に理由を記載できるように(JUICE独自)。絵文字申請・アバターデコレーション申請の却下理由と同様、却下されたメールアドレス宛てのメール・登録審査状況確認画面(引換コード)の両方で理由を確認できる
- Enhance: 管理画面のモデレーションログに、承認式新規登録・絵文字申請・アバターデコレーション申請の承認/却下の詳細(対象ユーザー・却下理由等)を表示するように(JUICE独自)。今までもログ自体は記録されていたが、種別ラベルのみで詳細が表示されていなかった
- Enhance: 管理画面の絵文字申請・アバターデコレーション申請の審査一覧に、審査待ちだけでなく承認済み/却下済みの履歴も閲覧できる状態切り替えを追加(JUICE独自)。あわせて審査済みの申請に誰がいつ審査したかを表示するように(通報機能の担当者表示と同様、モデレーターの特定は管理画面内のみで、申請者自身には公開しない)
- Enhance: 管理画面の承認待ちユーザー(承認式新規登録)にも、絵文字申請・アバターデコレーション申請と同様に承認済み/却下済みの履歴を閲覧できる状態切り替えを追加(JUICE独自)。却下されたアカウントは物理削除される仕様のため、申請時点のユーザー名・登録理由をスナップショットとして保持し、削除後も履歴から確認できるようにした。既存の承認済みユーザーについても、モデレーションログから審査したモデレーター・審査日時を遡って補完している
- Fix: 絵文字申請の「自分の申請一覧」・管理画面の審査カードで、申請時に設定したタグ(別名)・センシティブ・ローカル限定の各項目が表示されていなかった問題を修正。申請フォームでは入力できていたが、一覧・審査画面側に表示する項目が漏れていた
- Enhance: JUICE独自の項目に付く「JUICE」バッジのスタイルを、塗りつぶしから枠線のみのスケルトンスタイルに変更(JUICE独自)。塗りつぶしだと主張が強すぎるため
- Fix: ノートに既についている他人のリアクションをクリックして自分も同じリアクションを付ける「相乗り」が、実装が未完成のまま放置されており、権限(ロール制限・センシティブ・ローカル限定)を無視していたほか、リモートサーバーのカスタム絵文字によるリアクションには一切相乗りできなかった問題を修正(misskey-tempuraを参考に実装)。リモートリアクションへの相乗りはJUICE設定で有効化できる(他サーバーの絵文字画像を著作権者の許諾なく使用することになりうるため、既定は無効・管理者の自己責任でのオプトイン)
- Enhance: リアクションの絵文字情報(ライセンス等)を確認できる「情報」メニューを、今までローカルのカスタム絵文字のみだったのを、リモートサーバーのカスタム絵文字によるリアクションでも使えるように(JUICE独自)
- Feat: 投稿フォームに、投稿の言語(BCP 47言語タグ)を明示的に指定できるオプションを追加(JUICE独自、Mastodon互換)。未指定の場合は表示言語の設定が既定値として使われる
- Feat: JUICE設定に、ホーム・ローカル・グローバルタイムラインへ表示する投稿を言語で絞り込める設定を追加(JUICE独自、Mastodonの「フィルタする言語」を参考に実装)。Mastodon本家はホームタイムラインのみが対象でリノートはフィルタされない(既知のバグ)のに対し、JUICEでは全タイムラインに適用し、リノートもリノート元ノートの言語で判定する。言語が指定されていない投稿は、この設定に関わらず常に表示され、自分自身の投稿も他のタイムラインフィルターと同様に常に表示される
- Enhance: タイムライン画面の「…」メニューからも、上記の言語フィルタを直接切り替えられるように(JUICE独自)。リレーTLの絞り込みと同様、JUICE設定ページまで移動しなくてもその場で調整できる
- Feat: ノート検索・リアクション検索の高度な検索オプションに、投稿の言語(BCP 47言語タグ)での絞り込みを追加(JUICE独自)
- Feat: 絵文字申請・アバターデコレーション申請に「差し替え申請」を追加(JUICE独自)。新しい絵文字/デコレーションとしてではなく、自分の過去の承認済み申請から作成された既存の絵文字/デコレーションの画像だけを差し替えられる。差し替え対象にできるのは申請者自身が作成したものに限られ、名前・カテゴリ等のメタデータは変更されない。審査画面では差し替え前後の画像を比較表示する
- Fix: お知らせのリアクション・投票が、実際には他のユーザーの操作が画面に反映されずリロードするまで気づけなかった問題を修正。サーバー側は元々全クライアントへリアルタイム通知を送信していたが、受け取って画面に反映するクライアント側の実装が存在しなかった(結果としてこの通知は誰にも使われないまま毎回全接続クライアントへ送信され続けており、無駄なサーバー負荷にもなっていた)。お知らせ一覧・個別ページの両方で、他のユーザーがリアクション・投票した際にリアルタイムで反映されるように
- Feat: お問い合わせフォームを追加(JUICE独自、misskey-tempuraを参考に実装)。ログイン不要(管理者設定で認証を必須にすることも可能)で、カテゴリ・返信方法(メールアドレス/Misskeyユーザー名)を選んで送信できる。管理画面ではステータス管理(未対応/対応中/対応済み/クローズ)・管理者メモ・担当者割り当て・カテゴリ管理が可能で、受信時はSystemWebhook経由で通知できる
- Enhance: JUICE独自機能のUI文言に、英語・韓国語・簡体字中国語の翻訳を追加。このフォークにはCrowdinの自動同期が実装されていないため、本家に存在しないJUICE独自キーに限定して直接追記した(本家と共通のキーには一切手を加えていない)。今まではこれらの言語でもJUICE独自部分の表示は日本語のままだった
- Fix: 管理画面のモデレーションログで、承認式新規登録・絵文字申請・アバターデコレーション申請の承認/却下(6種)にJUICEバッジが表示されていなかった問題を修正。あわせて`updateJuiceSettings`(JUICE設定の変更)・`cleanupOrphanedObjectStorageFiles`(孤立したオブジェクトストレージファイルの整理)の2種は、アイコン・詳細表示・一覧の種別フィルタが一切対応しておらず(`misskey-js`側の型定義漏れが原因)、ログ自体は記録されていても内容を確認できていなかったため、あわせて対応した
- Fix: 「お問い合わせ」ページから新しいお問い合わせフォームへのボタンが、リンクとして機能しておらずクリックしても何も起きなかった問題を修正
- Enhance: お問い合わせフォームに、OS・ブラウザ・画面サイズ等のデバイス情報を任意で本文に含められるオプションを追加(JUICE独自)。技術的な不具合報告の解決に役立つことを想定
- Fix: アバターデコレーション申請フォーム・審査画面のプレビューにある角度・位置調整スライダーが、装着ダイアログ版に比べて不必要に細く表示されていた問題を修正(JUICE独自)
- Fix: お問い合わせフォームで、ログイン中に「Misskeyのユーザー名宛てに返信」欄へ自動入力される値が`ユーザー名@インスタンスのホスト名`形式になっており、インスタンスのURLがIPアドレスやポート番号を含む場合(開発環境等)にドメイン形式のバリデーションへ引っかかり送信ボタンが有効化されないままになる問題を修正。ローカルユーザーはホスト名を付けず`@ユーザー名`のみで送信できるようにした(MFMの@メンション記法もホスト省略でローカル扱いになるため)。あわせて送信完了画面のユーザー名表示が`@@ユーザー名`のように`@`が二重になっていた表示崩れも修正
- Feat: 絵文字申請・アバターデコレーション申請・承認式新規登録と同様に、お問い合わせの処理(ステータス変更・担当者割り当て・削除)もモデレーター以外にロール単位で個別に許可できるように(JUICE独自)。ロールポリシーに`canProcessContactForms`を追加(既定値false)し、「ツール」メニューにも専用リンクを追加。問い合わせ内容にはメールアドレス・IPアドレスといったPIIが含まれるため、モデレーター/管理者以外の担当者にはこれらをマスクして表示する(処理そのものは委譲するが、個人情報の閲覧はモデレーター/管理者に限定)
- Feat: JUICE設定に、お問い合わせ内容(本文)の最大文字数を変更できる設定を追加(JUICE独自、既定10000文字、20〜10000の範囲で設定可能)。今まで10000文字固定だった
- Feat: 絵文字申請・アバターデコレーション申請・承認式登録申請と同様に、お問い合わせが送信された際もコントロールパネルへ警告バナーを表示し、開いている間はadminストリーム経由でリアルタイムトースト通知も届くように(JUICE独自)。リアルタイム通知にはメールアドレス・IPアドレス等のPIIを含まない件名・カテゴリのみを載せる
- Feat: 起動時のロード画面に、misskey-tempuraを参考にしたsystemd風の起動ログ(画面左上、各起動ステップの開始・完了・失敗を表示)を追加(JUICE独自)
- Feat: JUICE設定に、起動時のロード画面でロゴの下へランダム表示する文言を設定できる項目を追加(JUICE独自、misskey-tempuraのcustomSplashTextを参考)。1行1件のテキストエリアで複数設定可能、未設定の場合は何も表示されない
- Feat: ホームタイムラインの「…」メニューに、ローカルユーザーの投稿だけに絞り込む切り替えを追加(JUICE独自)。フォロー中のリモートユーザーの投稿を一時的に非表示にしたい場合向け
- Feat: デッキUIのカラムに「お気に入り」を追加(JUICE独自)。既存の「お気に入り」ページ(/my/favorites)と同じ一覧を、他のカラムと並べて常設できる
- Fix: `MkSelect.vue`(コントロールパネルの各種プルダウン、デッキの「カラムを追加」等で使用)の選択肢に、本家に無いJUICE独自の項目であることを示すバッジを表示する仕組みが無かったため追加。デッキの「お気に入り」カラムをはじめ、今後のJUICE独自の選択肢一般で使えるようにした
- Fix: 設定の「JUICE」ページで、「表示するリレー」「タイムラインに表示する言語」がそれぞれ全項目を常に展開表示しており(対応言語は40件超)、設定画面が非常に長くなって他の設定項目を探しづらくなっていた問題を修正(JUICE独自)。折りたたみ式にし、見出しに現在の選択状況(「全て」/「N件選択中」)を表示するようにした
- Enhance: JUICE設定の起動画面の表示文言(customSplashText)に、行数(最大20行)・1行あたりの文字数(最大256文字)のクライアント側バリデーションを追加(JUICE独自)。今まではサーバー側の上限のみで、超過時に保存ボタンを押すまでエラーに気づけなかった
- Enhance: 絵文字申請・アバターデコレーション申請ページの「自分の申請一覧」タブを、「審査待ち」「結果」の2タブに分割(JUICE独自)。結果タブには承認済み/却下済みを切り替えられる状態フィルタを追加した。今までは全ステータスの申請が1つの一覧に混在しており、審査待ちのものを探しづらかった
- Fix: 絵文字申請・アバターデコレーション申請の一覧・審査画面で、カテゴリ・タグ・ライセンス・説明が未入力の場合に項目自体が表示されず、入力し忘れなのか元々空欄なのか分かりにくかった問題を修正。未入力時は「なし」と表示するように統一した
- Enhance: 絵文字申請・アバターデコレーション申請フォームで「PCからアップロード」を選んだ場合、選択した時点ではDriveへアップロードせず、ローカルでプレビューだけ行い、実際に「申請する」を押した瞬間にアップロードするように変更(JUICE独自)。今までは画像を選んだ時点で即Driveへアップロードされており、プレビューを見て申請をやめた場合や誤って複数回選び直した場合に、使われないファイルがDriveに残り続けていた。「ドライブから選択」「URLから」は従来通り、選択した時点で参照/アップロードする
- Feat: 絵文字申請・アバターデコレーション申請の承認時に、モデレーターが申請内容(絵文字は名前・カテゴリ・タグ・ライセンス・センシティブ/ローカル限定、アバターデコレーションは名前・説明・カテゴリ)を編集できるように(JUICE独自)。編集した場合は理由の入力が必須で、編集前の内容・編集理由はモデレーションログに記録される。差し替え申請(既存の絵文字/デコレーションの画像だけを差し替える申請)ではメタデータ自体を使わないため編集不可
- Enhance: コントロールパネルのStatsに、アバターデコレーション・ロール・ファイルの登録件数を表示するように(JUICE独自)。今まで投稿数・ユーザー数・インスタンス数・カスタム絵文字数・オンライン数のみだった

### Server
- Fix: サーバー名・短縮名・説明文にHTMLタグを含む文字列を設定していると、PWAとしてインストールした際のアイコン名(manifest.jsonのname/short_name)、ブラウザ検索バーへの追加時(opensearch.xml)、`/embed/*`ページの`<title>`・`og:site_name`、ノートのURLをMisskeyへ貼り付けた際に生成されるプレビューカードの`og:description`等、プレーンテキストとして表示されるべき場所にタグの記号がそのまま表示されてしまう問題を修正。あわせてopensearch.xmlが無加工の文字列結合でXMLを組み立てていた(不正なXMLになりうる)箇所も適切にエスケープするように修正。サーバー情報ページ等、意図的にHTMLとして表示している説明文欄自体の表示はそのまま
- Fix: JUICE独自のユーザーランキング(投稿数・リアクション数)が、ActivityPub経由で受信したリモートユーザーの投稿・リアクションまで加点しており、インスタンス単位のつもりが実質フェディバース全体のランキングになっていた問題を修正。ローカルユーザーの活動のみを対象とするように
- Feat: /api/emoji-requests/create-many、/api/avatar-decoration-requests/create-many を追加(JUICE独自)。複数件の申請をキャプチャ検証1回でまとめて作成できる(hCaptcha等のトークンは使い切りのため、既存の単体作成エンドポイントを複数回呼ぶ方式は取れない)。1件でも無効なファイルがあれば全体を作成しない、ロールポリシーの同時申請数上限をバッチ全体で超える場合は全体を拒否する、といった形で単体版と一貫した挙動にしている
- Fix: リアクション作成処理(ReactionService)が、リモートホストのカスタム絵文字を指定したリアクション文字列(`:name@host:`)を一切認識しておらず、既定のリアクションへ問答無用にフォールバックしていた問題を修正(misskey-tempuraを参考に実装)。ローカルユーザーからのリアクションに限り、指定されたホスト・ローカル・ノート投稿者のホストの順で、サーバーが既に把握している絵文字を探すようにした(リモートから受信したリアクションは、なりすまし防止のため従来通りリアクションした本人のホストのみを対象にする)。JUICE設定で無効化した場合は、ローカルユーザーからのリアクションもリアクションした本人のホスト(=ローカル)の絵文字のみを対象にする従来の挙動のまま
- Feat: /api/emoji に、リモートのカスタム絵文字の詳細情報(ライセンス等)を取得できるよう`host`パラメータを追加(JUICE独自)。今までローカルの絵文字のみ対応していた
- Feat: ノートに言語(BCP 47言語タグ)を付与できるように(JUICE独自)。/api/notes/createにlangパラメータを追加し、未指定の場合は投稿者の表示言語設定が既定値として使われる。ActivityPubでの連合にはAS2標準のcontentMapを使用(Mastodon/Akkoma互換)
- Feat: /api/notes/searchにlangパラメータを追加(JUICE独自)。ノートの言語(BCP 47言語タグ)との完全一致で絞り込める。sqlLike/sqlPgroonga・meilisearchの両プロバイダに対応
- Feat: /api/emoji-requests/create、/create-many、/api/avatar-decoration-requests/create、/create-manyにtargetEmojiId/targetAvatarDecorationIdパラメータを追加(JUICE独自)。指定すると「差し替え申請」となり、承認時は新規作成の代わりに対象の画像のみを差し替える。対象は申請者自身の承認済み申請から作られたものに限定され、同一対象への差し替え申請の重複は拒否する。あわせて/api/emoji-requests/list、/api/avatar-decoration-requests/listにstatusでの絞り込みパラメータを追加
- Feat: 絵文字申請・アバターデコレーション申請の承認/却下結果を、アプリ内通知でも受け取れるように(JUICE独自)。今まではメール通知(メアド未設定/未認証、または「結果をメールで受け取る」設定をオフにしていると届かない)のみだったため、通知欄からも承認/却下に気づけるようにした。他の通知種別と同様、設定画面から個別にON/OFFを切り替え可能
- Feat: /api/contact-form/submit、/api/admin/contact-form/{list,show,update,delete} を追加(JUICE独自、misskey-tempuraを参考に実装)。設定はmeta本体ではなくJUICE独自のJuiceSettingsに集約している。SystemWebhookに新しいイベント種別`receivedContactForm`を追加
- Fix: 絵文字申請・アバターデコレーション申請の承認/却下、承認式新規登録の承認/却下について、同一の申請・ユーザーに対して2件の審査リクエストがほぼ同時に処理されると、両方とも成功してしまう(状態の読み取りから書き込みまでの間に排他制御が無かった)問題を修正。条件付きUPDATE/DELETEで原子的に排他するようにした
- Fix: 承認式新規登録を却下した際、審査履歴の却下済み一覧に反映されない問題を修正(JUICE独自)。ユーザー行の削除で`signup_approval_check.userId`が外部キーの`ON DELETE SET NULL`によって先にNULL化されてしまい、その後に同じ`userId`で検索して行っていた履歴更新(status: 'declined')が対象0件のまま失敗していたため、ユーザー削除前に履歴レコードのidを控えて更新するように変更した
- Fix: 絵文字申請・アバターデコレーション申請の新規作成(create・create-many、計4エンドポイント)で、ロールポリシーの同時申請数上限(既定3件)のチェックとDB登録が別トランザクションになっており、複数リクエストをほぼ同時に送ることで上限を超えてpending申請を作成できたTOCTOUを修正。ユーザー単位のPostgres advisory lockでチェックと登録を直列化・同一トランザクション化した
- Fix: お知らせの単一選択(複数選択不可)投票で、投票済みチェックとDB登録のTOCTOUにより、複数の選択肢へほぼ同時に投票することで1人が複数票を投じられてしまう問題を修正。上記の絵文字/アバターデコレーション申請と同じPostgres advisory lockの手法で解消
- Fix: 未認証でも呼べるノート検索API(`/api/notes/search`)の`excludeWords`パラメータに件数・文字数の上限が無く、大量の除外ワードを渡すことでDBに重いクエリを組ませられた問題を修正(DoS対策)
- Fix: システムメールで、絵文字申請・アバターデコレーション申請・承認式新規登録の却下理由や申請名をHTML本文に埋め込む際、エスケープしていなかったためHTMLインジェクションが可能だった問題を修正。バックエンドの`I18n.t()`が、HTMLメールテンプレート(`_email.*.html`等)向けの値だけを自動的にエスケープするように変更し、あわせてメール件名を`<h1>`へ埋め込む箇所にもエスケープを追加した(プレーンテキストメール側の表示はそのまま)
- Fix: /api/contact-form/submitで、返信方法「Misskeyのユーザー名宛てに返信」のユーザー名指定が`ユーザー名@ホスト名`形式でないと拒否されており、ローカルユーザーが`@ユーザー名`のみで指定できなかった問題を修正。ホスト名を省略した場合はローカルユーザー宛てとして受け付けるようにした
- Feat: ロールポリシーに`canProcessContactForms`を追加(JUICE独自、既定値false)。モデレーター/管理者は引き続き常に全て処理できる。/api/admin/contact-form/{list,show,update,delete,categories}をこのロールポリシー(またはモデレーター)経由で許可するように変更し、モデレーター以外の担当者にはメールアドレス・IPアドレスをマスクして返す(`ContactFormEntityService.pack()`に`maskPii`オプションを追加)
- Feat: JUICE設定に`contactFormContentMaxLength`を追加(JUICE独自、既定10000)。/api/contact-form/submitのcontentパラメータがこの値を超える場合、`invalidContent`エラーで拒否する(paramDef側の10000は変更不可の絶対上限のまま)
- Feat: お問い合わせが送信された際、モデレーターへadminストリーム経由でリアルタイム通知するように(JUICE独自)。既存の絵文字申請・アバターデコレーション申請・承認式登録申請と同じ`JuiceAdminNotificationService`の枠組みに統合し、Webhook enqueue(`receivedContactForm`)もあわせて行うようにした(従来は`ContactFormService`から直接enqueueしていた)。通知ペイロードはPIIを含まない`{id, subject, category}`のみ
- Feat: JUICE設定に`customSplashText`を追加(JUICE独自、既定は空配列、misskey-tempuraのcustomSplashTextを参考)。起動時のロード画面(`_splash.tsx`)のロゴ下にランダムで1つ表示する。サーバー側で完結する設定のため、新規マイグレーションは追加せずJuiceSettingsのjsonbに載せている
- Feat: 起動時の生JSブートローダー(`public/loader/boot.js`)に、misskey-tempuraを参考にしたsystemd風の起動ログ機能を追加(JUICE独自)。言語検出・アプリ本体スクリプトの読み込み・テーマ適用・フォント設定適用・カスタムCSS適用の各ステップを画面左上に逐次表示する。`<head>`内で同期実行される都合上ほとんどのステップは`<body>`(および`#tty`)がまだ存在しないタイミングで完了してしまうため、該当する行のDOM要素はいったん保留キューに積んでおき、`#tty`が実際に使えるようになった時点(通常はDOMContentLoaded後のアプリ本体スクリプト読み込み時)でまとめて反映するようにし、最優先で即時開始すべきアプリ本体スクリプトの読み込み開始タイミングを一切遅延させずにログを取りこぼさないようにしている
- Feat: /api/notes/timeline、homeTimelineストリーミングチャンネルにlocalOnlyパラメータを追加(JUICE独自)。trueの場合、ホームタイムラインをローカルユーザーの投稿だけに絞り込む(自分自身の投稿は常に表示)

**Full Changelog**: https://github.com/Zel9278/misskey-juice/compare/v2026.7.0-juice+2.5...v2026.9.0-juice+3.0

## v2026.7.0-juice+2.5

- [絵文字申請](./emoji-request.md)・アバターデコレーション申請・[承認式新規登録](./approval-signup.md)の承認/却下を、モデレーター権限を持たないユーザーにもロール単位で個別に許可できるように

## v2026.7.0-juice+2.4

- 一般ユーザーがアバターデコレーションを申請できる「アバターデコレーション申請」ページを追加([絵文字申請](./emoji-request.md)と同じ仕組み)
- 管理画面のJUICE独自項目にバッジが表示されていなかった不具合を修正

## v2026.7.0-juice+2.3

- Webhookの送信先がDiscordのWebhook URLの場合、自動的に見やすいEmbed形式に整形して送信するように

## v2026.7.0-juice+2.2

- このリリースから`juice/main`ブランチへの公開を開始
- pgroonga検索で、`OR`や`-`等の記号を含む単語だと検索が失敗する不具合を修正
- (chan-maiさんの貢献)絵文字申請承認時のファイル破損、サインイン時の承認待ちチェックのタイミングなどを修正

## v2026.7.0-juice+2.1

- アプリ内の[About JUICEページ](./about-page.md)にコントリビューター欄を追加

## v2026.7.0-juice+2.0

JUICE独自機能をまとめて追加した大型リリースです。主な追加機能:

- [承認式新規登録](./approval-signup.md)
- [AI生成物フラグ](./ai-generated-flag.md)
- [絵文字申請](./emoji-request.md)
- [ユーザーランキング](./user-ranking.md)
- [リレータイムライン](./relay-timeline.md)
- [ウィジェット表示位置設定](./widget-position.md)
- [お知らせの投票機能](./announcement-poll.md)
- [LaTeX(数式)表示](./latex.md)
- 他ユーザーへの自分専用ニックネーム機能
- ログイン失敗時の本人通知
- アプリ内の[About JUICEページ](./about-page.md)を新設

## v2026.7.0-juice+1.0

Misskey 2026.7.0をベースとした初回リリース。misskey-artから以下を移植:

- [センシティブ画像の表示に関する修正](./cw-image-blur-fix.md)
- [お知らせのリアクション機能](./announcement-reaction.md)
- 開発用DBの誤消去防止ガード
