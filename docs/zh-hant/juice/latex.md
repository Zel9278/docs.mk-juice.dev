# LaTeX(數學公式)顯示

在貼文中撰寫 LaTeX 格式的數學公式後,會被實際渲染顯示的功能。此功能是恢復了官方 Misskey 曾經擁有、在 Juice Server 上重新實作的功能。

## 撰寫方式

使用 MFM 的數學公式語法。

- 行內公式: 用 `\(` 和 `\)` 包裹
- 區塊公式: 用 `\[` 和 `\]` 包裹

## 注意事項

- 根據公式內容的不同,顯示可能會失敗,但這種情況不會導致頁面損壞,僅相應部分不會顯示。
- 此功能可能會在伺服器整體設定中被停用。停用時,公式原始碼將直接以程式碼形式顯示。
